// This is a minimal service worker. In a real app, you'd add caching strategies.
self.addEventListener('fetch', (event) => {
  // We are not intercepting fetch requests for now.
  // This file just needs to exist for the PWA to be installable.
});